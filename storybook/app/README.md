# App

This is shared Storybook stories for Web (Expo), Android, iOS, and macOS.

## Restrictions

Due to limitation of React Native Storybook configuration, the `stories` directory cannot be renamed to anyelse. The `stories` directory is required for React Native Storybook to lookup files.
